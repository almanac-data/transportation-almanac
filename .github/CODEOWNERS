# Code owners for almanac-data/almanac-template
#
# This CODEOWNERS is inherited by every vertical generated from this template.
# Until per-vertical stewards are recruited, the engine caretaker reviews all
# changes so every contributor PR auto-requests a reviewer.
#
# As stewards come on board, replace the catch-all with scoped ownership, e.g.:
#   /catalog/   @almanac-data/<vertical>-stewards
#   /schema/    @rudi193-cmd

*   @rudi193-cmd
